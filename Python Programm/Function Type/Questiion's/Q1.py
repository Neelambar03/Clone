# write a program to find the square of a number by using lambda function.

# Ans:

# Define a list of numbers
numbers = [1, 2, 3, 4, 5]

# Use a lambda function to find the square of each number
sq = list(map(lambda x: x**2, numbers))

# Print the squared numbers
print("Squared numbers:", sq)

